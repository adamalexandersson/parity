<?php

namespace App\Providers;

use Illuminate\Support\ServiceProvider;

/**
 * Theme integration for Sprout.
 *
 * Sprout auto-discovers components from app/View/Components and enqueues the
 * editor runtime. Use this provider to register transforms and extend editor
 * config for your theme.
 */
class SproutServiceProvider extends ServiceProvider
{
    public function boot(): void
    {
        // Register style transforms used in component schemas:
        //
        // Sprout::transforms()->register('imageUrl', function ($value) {
        //     return is_numeric($value) ? wp_get_attachment_url((int) $value) : $value;
        // });
        //
        // Extend Gutenberg editor config:
        //
        // add_filter('sprout/editor/config', function (array $config) {
        //     return array_merge($config, [
        //         'icons' => [],
        //     ]);
        // });
    }
}
